```javascript
// bugSolution.js
<div class="bg-gray-200 p-4 rounded-lg">
  <h2 class="text-lg font-medium mb-2">Solution: Using safelist</h2>
  <p>This demonstrates how to resolve the issue by adding the dynamic classes to the `safelist` option within the `tailwind.config.js` file.</p>
  <p>Adding dynamic classes to `safelist` prevents PurgeCSS from removing them even if they're not explicitly used in the templates.</p>
  <ul>
    <li>Refer to the `tailwind.config.js` file for the configuration.</li>
  </ul>
</div>
```